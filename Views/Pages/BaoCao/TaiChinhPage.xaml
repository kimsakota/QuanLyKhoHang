<Page x:Class="UiDesktopApp1.Views.Pages.BaoCao.TaiChinhPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UiDesktopApp1.Views.Pages.BaoCao"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
      xmlns:beh="clr-namespace:UiDesktopApp1.Behaviors"
      xmlns:viewmodels="clr-namespace:UiDesktopApp1.ViewModels.Pages.BaoCao"
      d:DataContext="{d:DesignInstance Type=viewmodels:TaiChinhViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      ui:Design.Background="{ui:ThemeResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
      ScrollViewer.CanContentScroll="False"
      Title="Báo cáo tài chính">

    <Page.Resources>
        <beh:NumberVnConverter x:Key="NumberVnConverter"/>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="16" Margin="0,0,0,20"
                BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1" CornerRadius="8" 
                Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Vertical" Grid.Column="0">
                    <TextBlock Text="Báo cáo tài chính" FontSize="20" FontWeight="Bold"/>
                    <TextBlock Text="Thống kê doanh thu và lợi nhuận" Foreground="Gray" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:CalendarDatePicker Date="{Binding StartDate, Mode=TwoWay}" 
                                           Width="140" 
                                           Margin="0,0,10,0"/>

                    <TextBlock Text="-" VerticalAlignment="Center" Margin="0,0,10,0"/>

                    <ui:CalendarDatePicker Date="{Binding EndDate, Mode=TwoWay}" 
                                           Width="140" 
                                           Margin="0,0,10,0"/>

                    <ui:Button Icon="{ui:SymbolIcon ArrowClockwise24}" 
                               Appearance="Primary"
                               Command="{Binding LoadDataCommand}"
                               ToolTip="Cập nhật dữ liệu"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="8" Padding="20"
                    Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Tổng doanh thu" Foreground="Gray" FontWeight="Medium"/>
                        <TextBlock Text="{Binding TotalRevenue, StringFormat={}{0:N0} đ}" 
                                   FontSize="24" FontWeight="Bold" Margin="0,8,0,0"
                                   Foreground="CornflowerBlue"/>
                    </StackPanel>
                    <ui:SymbolIcon Symbol="Money24" Grid.Column="1" FontSize="32" Foreground="CornflowerBlue" VerticalAlignment="Top"/>
                </Grid>
            </Border>

            <Border Grid.Column="2" CornerRadius="8" Padding="20"
                    Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Tổng chi phí" Foreground="Gray" FontWeight="Medium"/>
                        <TextBlock Text="{Binding TotalCost, StringFormat={}{0:N0} đ}" 
                                   FontSize="24" FontWeight="Bold" Margin="0,8,0,0"
                                   Foreground="IndianRed"/>
                    </StackPanel>
                    <ui:SymbolIcon Symbol="MoneyDismiss24" Grid.Column="1" FontSize="32" Foreground="IndianRed" VerticalAlignment="Top"/>
                </Grid>
            </Border>

            <Border Grid.Column="4" CornerRadius="8" Padding="20"
                    Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Lợi nhuận thực tế" Foreground="Gray" FontWeight="Medium"/>
                        <TextBlock Text="{Binding TotalProfit, StringFormat={}{0:N0} đ}" 
                                   FontSize="24" FontWeight="Bold" Margin="0,8,0,0"
                                   Foreground="ForestGreen"/>
                    </StackPanel>
                    <ui:SymbolIcon Symbol="ArrowTrending24" Grid.Column="1" FontSize="32" Foreground="ForestGreen" VerticalAlignment="Top"/>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" 
                BorderBrush="{ui:ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1" CornerRadius="8" 
                Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}"
                Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Biểu đồ biến động theo thời gian" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,15"/>

                <lvc:CartesianChart Grid.Row="1"
                                    Series="{Binding Series}"
                                    XAxes="{Binding XAxes}"
                                    YAxes="{Binding YAxes}"
                                    LegendPosition="Top">
                </lvc:CartesianChart>

                <ui:ProgressRing IsIndeterminate="True" 
                                 Grid.RowSpan="2"
                                 Width="50" Height="50"
                                 Visibility="{Binding IsBusy, Converter={x:Static beh:BooleanToVisibilityConverter.Instance}}"
                                 VerticalAlignment="Center" HorizontalAlignment="Center"/>
            </Grid>
        </Border>

    </Grid>
</Page>